version: "3.8"

services:
  go-app:
    container_name: streaming-service
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    volumes:
      - ./videos:/app/videos   
    restart: unless-stopped